scripts:

  build:
    cmd: elm make --debug src/Main.elm --output ex/main.js && cp src/cli.js ex/cli.js
    desc: Build the command-line interpreter

  parse:
    cmd: node ex/cli.js
    desc: Run the command-line parser
